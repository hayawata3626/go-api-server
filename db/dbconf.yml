test:
  driver: mymysql
  open: $DATABASE_URL
  # open: tcp:$DB_HOST:3306*kanban/$DB_USER/$DB_PASSWORD

development:
  driver: mymysql
  open: $DATABASE_URL

production:
  driver: mymysql
  open: $DATABASE_URL
